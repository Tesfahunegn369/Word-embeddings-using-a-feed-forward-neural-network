# Word-embeddings-using-a-feed-forward-neural-network
Implements word embeddings using a feed-forward neural network, covering softmax, forward propagation,  cross-entropy loss, backpropagation, and gradient descent. Trains the model to learn word representations  and visualizes embeddings in 3D using PCA.
